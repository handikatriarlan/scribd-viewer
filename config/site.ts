export type SiteConfig = typeof siteConfig;

export const siteConfig = {
  name: "Scribd Viewer",
  description: "A simple web application to view Scribd documents by converting standard Scribd URLs into embeddable URLs. This application can be used to view pages on Scribd that are limited by premium access by converting the document URL into an embeddable format.",
};

